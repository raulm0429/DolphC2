#include <Windows.h>
#include <string>

using namespace std;


struct output {
    string consol_out;
};

struct reg {
    string DesktopName;
};

struct Shell {
    string cmd;
};

struct PS {
    string arg;
};
